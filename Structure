Pool objects handler
    Array of pool objects

Objects instantiator
Objects lifeTime manager 

Pool object

Algorithm 
    Get object from pool
        if there is no available objects
            instantiate new
            start timer to destroy overflow object
        else
            activate available